/**
 * @format
 */

import { AppRegistry } from 'react-native';
import navigation from './res/navigation';
import { name as appName } from './app.json';

AppRegistry.registerComponent(appName, () => navigation);
